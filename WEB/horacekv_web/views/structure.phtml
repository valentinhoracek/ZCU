<?php

include_once ("basic.phtml");

/**
 * Create head of the site.
 */
head(
    $this->metadata['title'],
    $this->metadata['keywords'],
    $this->metadata['description'],
    $this->metadata['local_path']
);

/**
 * Call required controller.
 */
require ("views/$this->view.phtml");

/**
 * Create foot of the site.
 */
foot();
?>

